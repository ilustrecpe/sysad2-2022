---
- name: Working config for centos
  hosts:
    - centos
  vars_files:
    - config.yaml
  roles: 
    - centos-clone
    - centos-ftp
    - centos-motd
    - centos-nagios

- name: Working config for Debian
  hosts:
    - debian
  vars_files:
    - config.yaml
  roles:
    - debian-clone
    - debian-ftp
    - debian-motd
    - debian-nagios
    
- name: Working config for Fedora
  hosts:
    - fedora
  vars_files:
    - config.yaml
  roles:
    - fedora-clone
    - fedora-ftp
    - fedora-motd
    - fedora-nagios
